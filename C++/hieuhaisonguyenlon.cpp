#define ll long long
#include<bits/stdc++.h>
using namespace std;

main() {
    int t;
    cin >> t;
    while (t--) {
        string a,b;
        getline(cin,a);
        getline(cin,b);
        int x=a.length();
        int y=b.length();
        
    }
}